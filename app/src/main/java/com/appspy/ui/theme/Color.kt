package com.appspy.ui.theme

import androidx.compose.ui.graphics.Color

// ── 主色 ─────────────────────────────────────────────────────
val Primary     = Color(0xFF4F46E5)   // 靛蓝
val PrimarySoft = Color(0xFFEEF0FF)

// ── 辅色 ─────────────────────────────────────────────────────
val Coral       = Color(0xFFF2683C)   // 珊瑚橙（第二条折线 / 排行超长条）
val CoralSoft   = Color(0xFFFFF4EE)

// ── 成功色 ───────────────────────────────────────────────────
val GreenOk     = Color(0xFF16A06A)
val GreenSoft   = Color(0xFFEAF7F1)

// ── 文字 ─────────────────────────────────────────────────────
val Ink         = Color(0xFF1B1C28)   // 正文
val InkSub      = Color(0xFF7A7C8A)   // 次要文字

// ── 背景 / 分隔 ───────────────────────────────────────────────
val Background  = Color(0xFFEEEFF4)   // 页面底色
val Surface     = Color(0xFFFFFFFF)   // 卡片 / 屏幕面
val Divider     = Color(0xFFECEDF2)   // 分隔线
val Chip        = Color(0xFFF4F5F9)   // 未选中 chip 背景
val NavBg       = Color(0xFFFAFAFC)   // 底部导航条背景
